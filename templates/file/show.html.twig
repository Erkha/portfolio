{% extends 'adminBase.html.twig' %}

{% block title %}File{% endblock %}

{% block body2 %}
    <h1>File</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ file.id }}</td>
            </tr>
            <tr>
                <th>FileName</th>
                <td>{{ file.FileName }}</td>
            </tr>
        </tbody>
    </table>

    <img src="/uploads/files/{{file.fileName}}" alt="" class="img-fluid">

    <a href="{{ path('file_index') }}">back to list</a>

    <a href="{{ path('file_edit', {'id': file.id}) }}">edit</a>

    {{ include('file/_delete_form.html.twig') }}
{% endblock %}
